var P = require('..');

module.exports = function textPort (el) {
    var $el = $(el);    

    return P.port(
        function setter (val) {
            $el.text(val);
        },
        function getter () {
            return $el.text();
        }
    );
}
