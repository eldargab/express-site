var P = require('../port');

module.exports = function textPort (el) {
    var $el = $(el);    

    return P.create(
        function setter (val) {
            $el.text(val);
        },
        function getter () {
            return $el.text();
        }
    );
}
