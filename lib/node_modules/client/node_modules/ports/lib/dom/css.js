var P = require('../port');

module.exports = function cssPort (el, klass) {
    var $el = $(el);

    return P.create(
        function setter (val) {
            $el.toggleClass(klass, val);
        },
        function getter () {
            return $el.hasClass(klass);
        }
    );
}
