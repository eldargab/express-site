module.exports = require('micro-templates').addHelper(
    function render (t, v1, v2, v3) {
        print(require(t)(v1, v2, v3));
    },
    'var Helpers = require("templates/helpers");',
    function H (helper, v1, v2, v3) {
        print(Helpers[helper](v1, v2, v3));
    }
);
